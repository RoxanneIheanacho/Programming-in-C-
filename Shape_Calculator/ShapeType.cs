namespace examination_2
{
    public enum ShapeType
    { //enum is an enumerable that is typically put in separate file, it declares the ShapeTypes.
    //can call an instance method of all 5 ShapeTypes
        Cuboid, Cylinder, Ellipse, Rectangle, Sphere
    }
}